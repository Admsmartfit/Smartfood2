# Renomeie este arquivo para .env e ajuste os valores
# O start.sh lê estas variáveis automaticamente

# Chave secreta para as sessões (MUDE ANTES DE USAR EM PRODUCAO)
SECRET_KEY=smartfood-ops-360-dev-key-mude-em-producao!!

# Porta do servidor (padrão: 8000)
PORT=8000

# Host (0.0.0.0 = aceita conexões de qualquer IP da rede local)
HOST=0.0.0.0
