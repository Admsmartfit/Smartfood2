diff --git a/__pycache__/label_service.cpython-313.pyc b/__pycache__/label_service.cpython-313.pyc
index ba57043..625ab02 100644
Binary files a/__pycache__/label_service.cpython-313.pyc and b/__pycache__/label_service.cpython-313.pyc differ
diff --git a/__pycache__/main.cpython-313.pyc b/__pycache__/main.cpython-313.pyc
index f0a4965..03586cb 100644
Binary files a/__pycache__/main.cpython-313.pyc and b/__pycache__/main.cpython-313.pyc differ
diff --git a/install_linux.sh b/install_linux.sh
index 12cf5d8..33ff0a7 100644
--- a/install_linux.sh
+++ b/install_linux.sh
@@ -32,6 +32,7 @@ if ! id "$APP_USER" &>/dev/null; then
 else
     echo "     Usu├írio '${APP_USER}' j├í existe."
 fi
+usermod -aG lp "$APP_USER" || true
 
 # ÔöÇÔöÇ 4. Copia arquivos do projeto ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
 echo ""
diff --git a/label_service.py b/label_service.py
index afc2e28..4489e9c 100644
--- a/label_service.py
+++ b/label_service.py
@@ -11,6 +11,8 @@ At print time, mm values are converted to dots (203 DPI = ~8 dots/mm).
 
 import json
 import socket
+import subprocess
+import shutil
 from datetime import datetime, timedelta
 from io import BytesIO
 
@@ -172,31 +174,79 @@ def generate_tspl(template_data: dict, print_data: dict, quantity: int = 1) -> s
     return "\r\n".join(lines) + "\r\n"
 
 
-# ÔöÇÔöÇ TCP socket send ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+# ÔöÇÔöÇ TCP / CUPS local printing send ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+def _is_cups_printer(printer_name: str) -> bool:
+    """Check if the given name is listed as a CUPS printer."""
+    if not shutil.which("lpstat"):
+        return False
+    try:
+        res = subprocess.run(["lpstat", "-v"], capture_output=True, text=True, timeout=3)
+        if res.returncode == 0:
+            for line in res.stdout.splitlines():
+                parts = line.split()
+                if len(parts) >= 3 and parts[0] == "device" and parts[1] == "for":
+                    name = parts[2].rstrip(":")
+                    if name == printer_name:
+                        return True
+    except Exception:
+        pass
+    return False
+
+
+def _print_via_cups(printer_name: str, data: bytes) -> tuple[bool, str]:
+    """Print raw bytes to a CUPS printer using the lp command."""
+    if not shutil.which("lp"):
+        return False, "Comando 'lp' do CUPS n├úo encontrado no sistema Linux."
+    try:
+        p = subprocess.Popen(
+            ["lp", "-d", printer_name, "-o", "raw"],
+            stdin=subprocess.PIPE,
+            stdout=subprocess.PIPE,
+            stderr=subprocess.PIPE
+        )
+        stdout, stderr = p.communicate(input=data, timeout=8)
+        if p.returncode == 0:
+            msg = stdout.decode("utf-8", errors="replace").strip()
+            return True, f"Enviado para a impressora local CUPS '{printer_name}' con sucesso. {msg}"
+        else:
+            err = stderr.decode("utf-8", errors="replace").strip()
+            return False, f"Erro ao enviar para CUPS '{printer_name}': {err}"
+    except Exception as e:
+        return False, f"Erro ao iniciar o subprocesso lp: {e}"
+
 
 def send_to_printer(ip: str, port: int, command: str) -> tuple[bool, str]:
     """
-    Open a TCP connection to the printer, send the command string, close.
+    Open a TCP connection to the network printer, or send to a local CUPS printer.
     Returns (success: bool, message: str).
-
-    Elgin L42 Pro specifics:
-    - Port 9100 (RAW TCP)
-    - Needs SHUT_WR signal so the printer knows the job is complete
-    - UTF-8 encoding (firmware >= 2023); falls back to latin-1 if encode fails
     """
     try:
         data = command.encode("utf-8")
     except Exception:
         data = command.encode("latin-1", errors="replace")
 
+    # Determine if printing to a CUPS printer or raw TCP Socket
+    use_cups = False
+    if shutil.which("lp") and ip:
+        if _is_cups_printer(ip):
+            use_cups = True
+        elif not any(c in ip for c in (".", ":")):
+            # Simple heuristic: no dots or colons indicates a queue name rather than IP/hostname
+            use_cups = True
+
+    if use_cups:
+        return _print_via_cups(ip, data)
+
+    # Fallback to TCP raw socket
     try:
         with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as sock:
             sock.settimeout(8)
             sock.connect((ip, int(port)))
             sock.sendall(data)
-            # Sinaliza FIN ao servidor para que a impressora saiba que o job acabou
+            # Signal FIN to socket so the printer knows the job is complete
             sock.shutdown(socket.SHUT_WR)
-            # Aguarda ACK/resposta (at├® 3s) ÔÇö opcional mas evita fechamento prematuro
+            # Wait for response (up to 3s) to prevent premature connection close
             try:
                 sock.recv(256)
             except Exception:
diff --git a/templates/labels.html b/templates/labels.html
index 6f82fb1..43ca89c 100644
--- a/templates/labels.html
+++ b/templates/labels.html
@@ -8,16 +8,17 @@
   /* ÔöÇÔöÇ Dot-grid canvas background ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
   .dot-grid {
     background-color: #141c2d;
-    background-image: radial-gradient(circle, rgba(255,255,255,.12) 1px, transparent 1px);
+    background-image: radial-gradient(circle, rgba(255, 255, 255, .12) 1px, transparent 1px);
     background-size: 18px 18px;
   }
 
   /* ÔöÇÔöÇ Label canvas (the white paper) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
   #label-canvas {
     background: #ffffff;
-    box-shadow: 0 8px 40px rgba(0,0,0,.6);
+    box-shadow: 0 8px 40px rgba(0, 0, 0, .6);
     position: relative;
-    overflow: visible;   /* text can bleed ÔÇö mirrors physical Zebra behaviour */
+    overflow: visible;
+    /* text can bleed ÔÇö mirrors physical Zebra behaviour */
     transition: width .2s, height .2s;
   }
 
@@ -34,13 +35,26 @@
     transition: outline .05s;
     transform-origin: top left;
   }
-  .canvas-field:hover        { outline: 1px dashed #93c5fd; outline-offset: 2px; }
-  .canvas-field.selected     { outline: 2px solid #3b82f6 !important; outline-offset: 2px; }
+
+  .canvas-field:hover {
+    outline: 1px dashed #93c5fd;
+    outline-offset: 2px;
+  }
+
+  .canvas-field.selected {
+    outline: 2px solid #3b82f6 !important;
+    outline-offset: 2px;
+  }
 
   /* QR field placeholder */
   .canvas-field.qr-field {
-    display: flex; align-items: center; justify-content: center;
-    border: 2px solid #000; font-size: 11px; font-weight: bold; color: #000;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    border: 2px solid #000;
+    font-size: 11px;
+    font-weight: bold;
+    color: #000;
     background: repeating-conic-gradient(#000 0% 25%, #fff 0% 50%) 0 0 / 6px 6px;
     opacity: .85;
   }
@@ -55,8 +69,15 @@
     cursor: pointer;
     transition: border-color .1s;
   }
-  .field-card.active { border-color: #3b82f6; box-shadow: 0 0 0 1px #3b82f6; }
-  .field-card:hover:not(.active) { border-color: #6b7280; }
+
+  .field-card.active {
+    border-color: #3b82f6;
+    box-shadow: 0 0 0 1px #3b82f6;
+  }
+
+  .field-card:hover:not(.active) {
+    border-color: #6b7280;
+  }
 
   /* Compact range slider */
   input[type="range"] {
@@ -80,24 +101,44 @@
     width: 52px;
     min-height: 0;
   }
-  .num-sm:focus { border-color: var(--blue); outline: none; }
+
+  .num-sm:focus {
+    border-color: var(--blue);
+    outline: none;
+  }
 
   /* Batch table ÔåÆ mobile cards */
   @media (max-width: 639px) {
-    .batch-table thead              { display: none; }
-    .batch-table tbody tr           {
-      display: block; background: var(--card);
-      border-radius: .75rem; margin-bottom: .5rem;
-      padding: .75rem 1rem; border: 1px solid var(--border);
+    .batch-table thead {
+      display: none;
     }
-    .batch-table tbody td           {
-      display: flex; justify-content: space-between; align-items: center;
-      padding: .2rem 0; border: none; font-size: .875rem;
+
+    .batch-table tbody tr {
+      display: block;
+      background: var(--card);
+      border-radius: .75rem;
+      margin-bottom: .5rem;
+      padding: .75rem 1rem;
+      border: 1px solid var(--border);
     }
-    .batch-table tbody td::before   {
-      content: attr(data-label); color: #6b7280;
-      font-size: .7rem; font-weight: 500; text-transform: uppercase;
-      letter-spacing: .04em; margin-right: .5rem;
+
+    .batch-table tbody td {
+      display: flex;
+      justify-content: space-between;
+      align-items: center;
+      padding: .2rem 0;
+      border: none;
+      font-size: .875rem;
+    }
+
+    .batch-table tbody td::before {
+      content: attr(data-label);
+      color: #6b7280;
+      font-size: .7rem;
+      font-weight: 500;
+      text-transform: uppercase;
+      letter-spacing: .04em;
+      margin-right: .5rem;
     }
   }
 </style>
@@ -127,14 +168,13 @@
   <!-- ÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉ
        WYSIWYG EDITOR
   ÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉÔòÉ -->
-  <div class="flex gap-0 rounded-xl overflow-hidden border border-gray-700"
-       style="min-height:640px">
+  <div class="flex gap-0 rounded-xl overflow-hidden border border-gray-700" style="min-height:640px">
 
     <!-- ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
          LEFT PANEL ÔÇö settings + field list
     ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ -->
     <div class="w-72 flex-shrink-0 overflow-y-auto flex flex-col"
-         style="background:var(--surface); border-right:1px solid var(--border)">
+      style="background:var(--surface); border-right:1px solid var(--border)">
 
       <!-- Template settings -->
       <div class="p-4 border-b border-gray-700 space-y-3">
@@ -166,8 +206,8 @@
 
         <div class="grid grid-cols-3 gap-2">
           <div class="col-span-2">
-            <label class="text-xs text-gray-500">IP da Impressora</label>
-            <input type="text" x-model="printerIp" placeholder="192.168.1.100" class="text-sm font-mono" />
+            <label class="text-xs text-gray-500">IP ou Fila (CUPS)</label>
+            <input type="text" x-model="printerIp" placeholder="Ex: L42PRO-FULL ou IP" class="text-sm font-mono" />
           </div>
           <div>
             <label class="text-xs text-gray-500">Porta</label>
@@ -185,17 +225,15 @@
 
         <!-- Existing field cards -->
         <template x-for="(f, i) in fields" :key="i">
-          <div class="field-card"
-               :class="selectedIdx === i ? 'active' : ''"
-               @click="selectedIdx = i">
+          <div class="field-card" :class="selectedIdx === i ? 'active' : ''" @click="selectedIdx = i">
 
             <div class="flex items-center justify-between mb-2">
               <div class="flex items-center gap-2">
                 <span class="text-sm" x-text="fieldIcon(f.type)"></span>
                 <span class="text-sm font-medium text-white" x-text="f.label"></span>
               </div>
-              <button @click.stop="removeField(i)"
-                      class="icon-btn text-xs hover:text-red-400" title="Remover">Ô£ò</button>
+              <button @click.stop="removeField(i)" class="icon-btn text-xs hover:text-red-400"
+                title="Remover">Ô£ò</button>
             </div>
 
             <!-- Controls (compact, always visible) -->
@@ -203,38 +241,31 @@
               <!-- X axis -->
               <div class="flex items-center gap-2">
                 <label class="text-xs text-gray-500 w-4">X</label>
-                <input type="range" min="0" :max="widthMm" step="0.5"
-                       x-model.number="f.x" class="flex-1 h-4" />
-                <input type="number" min="0" :max="widthMm" step="0.5"
-                       x-model.number="f.x" class="num-sm" />
+                <input type="range" min="0" :max="widthMm" step="0.5" x-model.number="f.x" class="flex-1 h-4" />
+                <input type="number" min="0" :max="widthMm" step="0.5" x-model.number="f.x" class="num-sm" />
               </div>
               <!-- Y axis -->
               <div class="flex items-center gap-2">
                 <label class="text-xs text-gray-500 w-4">Y</label>
-                <input type="range" min="0" :max="heightMm" step="0.5"
-                       x-model.number="f.y" class="flex-1 h-4" />
-                <input type="number" min="0" :max="heightMm" step="0.5"
-                       x-model.number="f.y" class="num-sm" />
+                <input type="range" min="0" :max="heightMm" step="0.5" x-model.number="f.y" class="flex-1 h-4" />
+                <input type="number" min="0" :max="heightMm" step="0.5" x-model.number="f.y" class="num-sm" />
               </div>
 
               <!-- Font size OR QR size -->
               <template x-if="!f.isQr">
                 <div class="flex items-center gap-2">
                   <label class="text-xs text-gray-500 w-8 flex-shrink-0">Fonte</label>
-                  <input type="range" min="1.5" max="12" step="0.5"
-                         x-model.number="f.font_size_mm" class="flex-1 h-4" />
-                  <input type="number" min="1.5" max="12" step="0.5"
-                         x-model.number="f.font_size_mm" class="num-sm" />
+                  <input type="range" min="1.5" max="12" step="0.5" x-model.number="f.font_size_mm"
+                    class="flex-1 h-4" />
+                  <input type="number" min="1.5" max="12" step="0.5" x-model.number="f.font_size_mm" class="num-sm" />
                   <span class="text-xs text-gray-600">mm</span>
                 </div>
               </template>
               <template x-if="f.isQr">
                 <div class="flex items-center gap-2">
                   <label class="text-xs text-gray-500 w-8 flex-shrink-0">QR sz</label>
-                  <input type="range" min="5" max="40" step="1"
-                         x-model.number="f.size" class="flex-1 h-4" />
-                  <input type="number" min="5" max="40" step="1"
-                         x-model.number="f.size" class="num-sm" />
+                  <input type="range" min="5" max="40" step="1" x-model.number="f.size" class="flex-1 h-4" />
+                  <input type="number" min="5" max="40" step="1" x-model.number="f.size" class="num-sm" />
                   <span class="text-xs text-gray-600">mm</span>
                 </div>
               </template>
@@ -244,10 +275,9 @@
                 <label class="flex items-center gap-2 cursor-pointer mt-1">
                   <div class="relative">
                     <input type="checkbox" x-model="f.bold" class="sr-only" />
-                    <div class="w-8 h-4 rounded-full transition"
-                         :class="f.bold ? 'bg-blue-600' : 'bg-gray-600'"></div>
+                    <div class="w-8 h-4 rounded-full transition" :class="f.bold ? 'bg-blue-600' : 'bg-gray-600'"></div>
                     <div class="absolute top-0.5 w-3 h-3 bg-white rounded-full shadow transition-transform"
-                         :class="f.bold ? 'translate-x-4' : 'translate-x-0.5'"></div>
+                      :class="f.bold ? 'translate-x-4' : 'translate-x-0.5'"></div>
                   </div>
                   <span class="text-xs text-gray-400">Negrito</span>
                 </label>
@@ -259,20 +289,16 @@
         <!-- Add field dropdown -->
         <div x-data="{open:false}" class="relative">
           <button @click="open=!open; $nextTick(() => $refs.addMenu && $refs.addMenu.focus())"
-                  class="btn btn-secondary btn-sm btn-full mt-1"
-                  style="border-style:dashed">
+            class="btn btn-secondary btn-sm btn-full mt-1" style="border-style:dashed">
             + Adicionar Campo
           </button>
-          <div x-show="open" x-cloak @click.outside="open=false"
-               x-ref="addMenu" tabindex="-1"
-               class="absolute left-0 right-0 mt-1 rounded-xl border border-gray-700 overflow-hidden z-20"
-               style="background:var(--card)"
-               x-transition:enter="transition ease-out duration-100"
-               x-transition:enter-start="opacity-0 scale-y-95 origin-top"
-               x-transition:enter-end="opacity-100 scale-y-100">
+          <div x-show="open" x-cloak @click.outside="open=false" x-ref="addMenu" tabindex="-1"
+            class="absolute left-0 right-0 mt-1 rounded-xl border border-gray-700 overflow-hidden z-20"
+            style="background:var(--card)" x-transition:enter="transition ease-out duration-100"
+            x-transition:enter-start="opacity-0 scale-y-95 origin-top" x-transition:enter-end="opacity-100 scale-y-100">
             <template x-for="af in availableFields" :key="af.type">
               <button @click="addField(af); open=false"
-                      class="w-full text-left px-3 py-2 text-sm hover:bg-gray-700/60 flex items-center gap-2 transition">
+                class="w-full text-left px-3 py-2 text-sm hover:bg-gray-700/60 flex items-center gap-2 transition">
                 <span x-text="fieldIcon(af.type)"></span>
                 <span x-text="af.label"></span>
               </button>
@@ -283,16 +309,15 @@
 
       <!-- Save / Update / Delete buttons -->
       <div class="p-4 border-t border-gray-700 space-y-2">
-        <button @click="saveTemplate()" :disabled="saving"
-                class="btn btn-primary btn-full text-sm">
-          <span x-text="saving ? 'SalvandoÔÇª' : (currentId ? '­ƒÆ¥ Atualizar Template' : '­ƒÆ¥ Salvar Novo Template')"></span>
+        <button @click="saveTemplate()" :disabled="saving" class="btn btn-primary btn-full text-sm">
+          <span
+            x-text="saving ? 'SalvandoÔÇª' : (currentId ? '­ƒÆ¥ Atualizar Template' : '­ƒÆ¥ Salvar Novo Template')"></span>
         </button>
         <div x-show="currentId" x-cloak class="flex gap-2">
           <button @click="newTemplate()" class="btn btn-secondary btn-sm flex-1">
             + Novo (em branco)
           </button>
-          <button @click="deleteTemplate()"
-                  class="btn btn-danger btn-sm px-3" title="Excluir template">
+          <button @click="deleteTemplate()" class="btn btn-danger btn-sm px-3" title="Excluir template">
             ­ƒùæ´©Å
           </button>
         </div>
@@ -300,8 +325,7 @@
 
       <!-- Saved templates list -->
       <div class="border-t border-gray-700">
-        <button @click="showTemplates = !showTemplates"
-                class="w-full flex items-center justify-between px-4 py-2.5 text-xs font-semibold
+        <button @click="showTemplates = !showTemplates" class="w-full flex items-center justify-between px-4 py-2.5 text-xs font-semibold
                        text-gray-400 uppercase tracking-wide hover:text-white hover:bg-gray-800/40 transition">
           <span>Templates Salvos (<span x-text="savedTemplates.length"></span>)</span>
           <span x-text="showTemplates ? 'Ôû▓' : 'Ôû╝'"></span>
@@ -309,8 +333,8 @@
         <div x-show="showTemplates" x-cloak class="pb-2">
           <template x-for="t in savedTemplates" :key="t.id">
             <button @click="loadTemplate(t)"
-                    class="w-full text-left px-4 py-2 text-xs hover:bg-gray-700/50 transition flex items-center gap-2"
-                    :class="currentId === t.id ? 'text-blue-400 bg-blue-900/20' : 'text-gray-400'">
+              class="w-full text-left px-4 py-2 text-xs hover:bg-gray-700/50 transition flex items-center gap-2"
+              :class="currentId === t.id ? 'text-blue-400 bg-blue-900/20' : 'text-gray-400'">
               <span>­ƒÅÀ´©Å</span>
               <span class="flex-1 truncate" x-text="t.name"></span>
               <span class="text-gray-600 flex-shrink-0" x-text="t.width_mm + '├ù' + t.height_mm"></span>
@@ -327,7 +351,7 @@
          RIGHT ÔÇö CANVAS AREA
     ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ -->
     <div class="flex-1 dot-grid flex flex-col items-center justify-start overflow-auto p-8 gap-4"
-         @click.self="selectedIdx = null">
+      @click.self="selectedIdx = null">
 
       <!-- Toolbar: preview data selector + zoom + instructions -->
       <div class="flex flex-wrap items-center gap-3 w-full max-w-2xl">
@@ -342,10 +366,10 @@
         </div>
         <div class="flex items-center gap-1">
           <button @click="zoom = Math.max(3, zoom - 1)"
-                  class="icon-btn text-gray-400 hover:text-white text-lg leading-none">ÔêÆ</button>
+            class="icon-btn text-gray-400 hover:text-white text-lg leading-none">ÔêÆ</button>
           <span class="text-xs text-gray-500 w-12 text-center" x-text="zoom + 'px/mm'"></span>
           <button @click="zoom = Math.min(12, zoom + 1)"
-                  class="icon-btn text-gray-400 hover:text-white text-lg leading-none">+</button>
+            class="icon-btn text-gray-400 hover:text-white text-lg leading-none">+</button>
         </div>
         <span class="text-xs text-gray-600 italic hidden lg:inline">
           Arraste os campos para reposicionar
@@ -353,16 +377,12 @@
       </div>
 
       <!-- THE LABEL CANVAS -->
-      <div id="label-canvas"
-           x-ref="canvas"
-           :style="`width:${canvasW}px; height:${canvasH}px`"
-           @click.self="selectedIdx = null">
+      <div id="label-canvas" x-ref="canvas" :style="`width:${canvasW}px; height:${canvasH}px`"
+        @click.self="selectedIdx = null">
 
         <template x-for="(f, i) in fields" :key="i">
           <div :class="'canvas-field' + (f.isQr ? ' qr-field' : '') + (selectedIdx === i ? ' selected' : '')"
-               :style="fieldStyle(f)"
-               @mousedown.prevent="startDrag($event, i)"
-               @click.stop="selectedIdx = i">
+            :style="fieldStyle(f)" @mousedown.prevent="startDrag($event, i)" @click.stop="selectedIdx = i">
             <template x-if="!f.isQr">
               <span x-text="sampleText(f.type)"></span>
             </template>
@@ -376,7 +396,8 @@
       <!-- Canvas size hint -->
       <p class="text-xs text-gray-600">
         <span x-text="widthMm"></span>mm ├ù <span x-text="heightMm"></span>mm
-        <span class="ml-2 opacity-60">(<span x-text="canvasW"></span>├ù<span x-text="canvasH"></span>px @ <span x-text="zoom"></span>px/mm)</span>
+        <span class="ml-2 opacity-60">(<span x-text="canvasW"></span>├ù<span x-text="canvasH"></span>px @ <span
+            x-text="zoom"></span>px/mm)</span>
       </p>
     </div>
   </div>
@@ -400,30 +421,25 @@
         <h2 class="text-xs font-semibold text-gray-400 uppercase tracking-wide mb-3">
           Registrar Lote de Produ├º├úo
         </h2>
-        <form hx-post="/batches"
-              hx-target="#batch-table-body"
-              hx-swap="afterbegin"
-              @htmx:after-request="if($event.detail.successful){ addBatchFromResponse($event, $refs.batchForm); ingredientsSummary=''; selectedRecipeId=''; }"
-              x-ref="batchForm"
-              class="grid grid-cols-2 sm:grid-cols-3 gap-3">
+        <form hx-post="/batches" hx-target="#batch-table-body" hx-swap="afterbegin"
+          @htmx:after-request="if($event.detail.successful){ addBatchFromResponse($event, $refs.batchForm); ingredientsSummary=''; selectedRecipeId=''; }"
+          x-ref="batchForm" class="grid grid-cols-2 sm:grid-cols-3 gap-3">
 
           <!-- Lote gerado automaticamente -->
           <div>
             <label class="text-xs text-gray-400 flex items-center justify-between">
               N┬║ do Lote
-              <button type="button" @click="generateBatchNumber()"
-                      class="text-blue-400 hover:text-blue-300 text-xs" tabindex="-1">Ôå╗ Novo</button>
+              <button type="button" @click="generateBatchNumber()" class="text-blue-400 hover:text-blue-300 text-xs"
+                tabindex="-1">Ôå╗ Novo</button>
             </label>
-            <input type="text" name="batch_number" x-model="batchNumber"
-                   class="font-mono text-sm" required readonly
-                   style="background:var(--bg)" />
+            <input type="text" name="batch_number" x-model="batchNumber" class="font-mono text-sm" required readonly
+              style="background:var(--bg)" />
           </div>
 
           <!-- Produto puxado das receitas -->
           <div>
             <label class="text-xs text-gray-400">Produto (Receita)</label>
-            <select name="product_name" required class="text-sm"
-                    @change="let opt = $event.target.options[$event.target.selectedIndex];
+            <select name="product_name" required class="text-sm" @change="let opt = $event.target.options[$event.target.selectedIndex];
                              selectedRecipeId = opt.dataset.id || '';
                              ingredientsSummary = recipesIngredients[opt.dataset.id] || '';">
               <option value="" data-id="">ÔÇö selecione ÔÇö</option>
@@ -437,13 +453,13 @@
           <!-- Validade calculada -->
           <div class="col-span-2 sm:col-span-1">
             <label class="text-xs text-gray-400">Validade (meses)</label>
-            <input type="number" x-model.number="validadeMeses" @input="calcExpiry()"
-                   min="1" max="60" class="text-sm" />
+            <input type="number" x-model.number="validadeMeses" @input="calcExpiry()" min="1" max="60"
+              class="text-sm" />
           </div>
           <div class="col-span-2">
             <label class="text-xs text-gray-400">Vencimento (calculado)</label>
-            <input type="date" name="expiry_date" x-model="expiryDate"
-                   class="text-sm" required readonly style="background:var(--bg)" />
+            <input type="date" name="expiry_date" x-model="expiryDate" class="text-sm" required readonly
+              style="background:var(--bg)" />
           </div>
 
           <div>
@@ -462,17 +478,16 @@
           <div class="col-span-2 sm:col-span-3">
             <label class="text-xs text-gray-400 flex items-center gap-1.5">
               Ingredientes (resumo p/ etiqueta)
-              <span x-show="ingredientsSummary" x-cloak
-                    class="text-xs text-green-500 font-normal">Ô£ô preenchido automaticamente</span>
+              <span x-show="ingredientsSummary" x-cloak class="text-xs text-green-500 font-normal">Ô£ô preenchido
+                automaticamente</span>
             </label>
-            <input type="text" name="ingredients_summary"
-                   x-model="ingredientsSummary"
-                   placeholder="Frango, Alho, Azeite, Sal" />
+            <input type="text" name="ingredients_summary" x-model="ingredientsSummary"
+              placeholder="Frango, Alho, Azeite, Sal" />
           </div>
 
           <!-- QR URLs -->
           <div class="col-span-2 sm:col-span-3 rounded-lg p-3 border border-gray-700 space-y-2"
-               style="background:var(--bg)">
+            style="background:var(--bg)">
             <p class="text-xs text-gray-400 font-medium">­ƒöù URLs QR Code Din├ómico</p>
             <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
               <div>
@@ -506,16 +521,13 @@
           <div class="flex flex-col lg:flex-row gap-5">
 
             <!-- Form column -->
-            <form hx-post
-                  :action="'/labels/' + (currentId || 0) + '/print'"
-                  hx-target="#print-result"
-                  hx-swap="innerHTML"
-                  @htmx:after-request="$event.detail.successful && toast('Enviado para impressora')"
-                  class="space-y-3 flex-1">
+            <form hx-post :action="'/labels/' + (currentId || 0) + '/print'" hx-target="#print-result"
+              hx-swap="innerHTML" @htmx:after-request="$event.detail.successful && toast('Enviado para impressora')"
+              class="space-y-3 flex-1">
               <div>
                 <label class="text-xs text-gray-400 block mb-1">Template</label>
                 <select name="template_select" class="text-sm w-full"
-                        @change="loadTemplateById(parseInt($event.target.value)); fetchPrintPreview()">
+                  @change="loadTemplateById(parseInt($event.target.value)); fetchPrintPreview()">
                   <option value="0">ÔÇö selecione ÔÇö</option>
                   <template x-for="t in savedTemplates" :key="t.id">
                     <option :value="t.id" :selected="currentId === t.id" x-text="t.name"></option>
@@ -524,8 +536,8 @@
               </div>
               <div>
                 <label class="text-xs text-gray-400 block mb-1">Lote</label>
-                <select name="batch_id" x-model="printBatchId" @change="fetchPrintPreview()"
-                        required class="text-sm w-full">
+                <select name="batch_id" x-model="printBatchId" @change="fetchPrintPreview()" required
+                  class="text-sm w-full">
                   <option value="">ÔÇö selecione ÔÇö</option>
                   <template x-for="b in batches" :key="b.id">
                     <option :value="b.id" x-text="b.batch_number + ' ÔÇö ' + b.product_name"></option>
@@ -534,40 +546,43 @@
               </div>
               <div>
                 <label class="text-xs text-gray-400 block mb-1">Quantidade de C├│pias</label>
-                <input type="number" name="quantity" x-model.number="printQty"
-                       min="1" max="5000" class="text-sm w-full font-bold text-blue-300" />
+                <input type="number" name="quantity" x-model.number="printQty" min="1" max="5000"
+                  class="text-sm w-full font-bold text-blue-300" />
               </div>
-              <button type="submit"
-                      :disabled="!currentId || !printBatchId || printQty < 1"
-                      class="btn btn-primary btn-full text-sm"
-                      :title="!currentId ? 'Selecione um template primeiro' : ''">
+              <button type="submit" :disabled="!currentId || !printBatchId || printQty < 1"
+                class="btn btn-primary btn-full text-sm" :title="!currentId ? 'Selecione um template primeiro' : ''">
                 ­ƒû¿´©Å Enviar para Impressora
               </button>
               <div id="print-result"></div>
             </form>
 
             <!-- Preview column -->
-            <div class="flex-1 dot-grid border border-gray-700 rounded-xl flex flex-col relative overflow-hidden" style="min-height:350px; max-height:600px">
+            <div class="flex-1 dot-grid border border-gray-700 rounded-xl flex flex-col relative overflow-hidden"
+              style="min-height:350px; max-height:600px">
 
-              <div class="absolute top-0 left-0 right-0 p-3 bg-gray-900/80 backdrop-blur-sm border-b border-gray-700 z-10 flex justify-between items-center">
+              <div
+                class="absolute top-0 left-0 right-0 p-3 bg-gray-900/80 backdrop-blur-sm border-b border-gray-700 z-10 flex justify-between items-center">
                 <span class="text-xs font-semibold text-gray-400 uppercase tracking-wide">Preview de Impress├úo</span>
-                <span x-show="printQty > 1" x-cloak class="text-xs font-bold text-blue-400 bg-blue-900/30 px-2 py-0.5 rounded-md">
+                <span x-show="printQty > 1" x-cloak
+                  class="text-xs font-bold text-blue-400 bg-blue-900/30 px-2 py-0.5 rounded-md">
                   <span x-text="printQty"></span> c├│pias
                 </span>
               </div>
 
-              <div x-show="!printPreviewHtml" class="flex-1 flex items-center justify-center text-center text-gray-500 text-xs p-6 mt-10">
-                Selecione um <strong class="text-gray-400 mx-1">Template</strong> e um <strong class="text-gray-400 mx-1">Lote</strong><br>para pr├®-visualizar a impress├úo.
+              <div x-show="!printPreviewHtml"
+                class="flex-1 flex items-center justify-center text-center text-gray-500 text-xs p-6 mt-10">
+                Selecione um <strong class="text-gray-400 mx-1">Template</strong> e um <strong
+                  class="text-gray-400 mx-1">Lote</strong><br>para pr├®-visualizar a impress├úo.
               </div>
 
-              <div x-show="printPreviewHtml" class="flex-1 overflow-y-auto p-6 pt-16 flex flex-wrap gap-4 justify-center content-start">
+              <div x-show="printPreviewHtml"
+                class="flex-1 overflow-y-auto p-6 pt-16 flex flex-wrap gap-4 justify-center content-start">
                 <template x-for="i in Math.min(parseInt(printQty) || 1, 10)" :key="i">
-                  <div x-html="printPreviewHtml"
-                       class="shadow-xl flex-shrink-0"
-                       style="transform:scale(0.85);transform-origin:top center;margin-bottom:-10px"></div>
+                  <div x-html="printPreviewHtml" class="shadow-xl flex-shrink-0"
+                    style="transform:scale(0.85);transform-origin:top center;margin-bottom:-10px"></div>
                 </template>
                 <div x-show="(parseInt(printQty) || 1) > 10"
-                     class="w-full text-center text-xs text-gray-500 mt-4 py-3 border border-dashed border-gray-700 rounded-lg bg-gray-800/30">
+                  class="w-full text-center text-xs text-gray-500 mt-4 py-3 border border-dashed border-gray-700 rounded-lg bg-gray-800/30">
                   + <span x-text="printQty - 10"></span> etiqueta(s) na fila de impress├úo...
                 </div>
               </div>
@@ -606,12 +621,12 @@
               <tbody id="batch-table-body">
                 {% for b in batches %}
                 <tr id="batch-{{ b.id }}" class="border-b border-gray-800">
-                  <td data-label="Lote"    class="py-1.5 px-1 text-gray-200 font-mono text-xs">{{ b.batch_number }}</td>
+                  <td data-label="Lote" class="py-1.5 px-1 text-gray-200 font-mono text-xs">{{ b.batch_number }}</td>
                   <td data-label="Produto" class="py-1.5 px-1 text-gray-200 text-xs">{{ b.product_name }}</td>
-                  <td data-label="Val."    class="py-1.5 px-1 text-gray-400 text-xs">{{ b.expiry_date.strftime('%d/%m/%Y') }}</td>
-                  <td data-label="QR"      class="py-1.5 px-1 text-center">
-                    <a href="/qr/{{ b.id }}" target="_blank"
-                       class="text-xs text-blue-400 hover:text-blue-300">Ôåù</a>
+                  <td data-label="Val." class="py-1.5 px-1 text-gray-400 text-xs">{{ b.expiry_date.strftime('%d/%m/%Y')
+                    }}</td>
+                  <td data-label="QR" class="py-1.5 px-1 text-center">
+                    <a href="/qr/{{ b.id }}" target="_blank" class="text-xs text-blue-400 hover:text-blue-300">Ôåù</a>
                   </td>
                 </tr>
                 {% endfor %}
@@ -629,350 +644,355 @@
 
 {% block scripts %}
 <script>
-// ÔöÇÔöÇ Sample data shown in canvas preview ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-const SAMPLE = {
-  product_name:        'Frango Grelhado',
-  batch_number:        'L-2024-001',
-  production_date:     '01/04/2024',
-  expiry_date:         '08/04/2024',
-  weight:              '1.250 kg',
-  ingredients_summary: 'Frango, Alho, Azeite, Sal',
-};
-
-const FIELD_ICONS = {
-  product_name: '­ƒôª', batch_number: '­ƒöó', production_date: '­ƒôà',
-  expiry_date: 'ÔÅ│', weight: 'ÔÜû´©Å', ingredients_summary: '­ƒÑù', qr_code: 'Ôûú',
-};
-
-// Available field types the user can add
-const AVAILABLE_FIELDS = [
-  { type: 'product_name',        label: 'Nome do Produto',      isQr: false, font_size_mm: 4.5, bold: true  },
-  { type: 'batch_number',        label: 'N├║mero do Lote',       isQr: false, font_size_mm: 3,   bold: false },
-  { type: 'production_date',     label: 'Data de Fabrica├º├úo',   isQr: false, font_size_mm: 3,   bold: false },
-  { type: 'expiry_date',         label: 'Data de Validade',     isQr: false, font_size_mm: 3,   bold: true  },
-  { type: 'weight',              label: 'Peso',                 isQr: false, font_size_mm: 3,   bold: false },
-  { type: 'ingredients_summary', label: 'Lista de Ingredientes',isQr: false, font_size_mm: 2.5, bold: false },
-  { type: 'qr_code',             label: 'QR Code Din├ómico',     isQr: true,  size: 15                       },
-];
-
-function labelMaker() {
-  return {
-    // ÔöÇÔöÇ Template state ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    currentId:   null,
-    tplName:     '',
-    widthMm:     62,
-    heightMm:    40,
-    printerType: 'ZPL',
-    printerIp:   '',
-    printerPort: 9100,
-    fields:      [],
-
-    // ÔöÇÔöÇ UI state ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    selectedIdx:      null,
-    saving:           false,
-    zoom:             7,        // px per mm
-    previewBatchIdx:  -1,
-    showTemplates:    true,
-    savedTemplates:      {{ templates_json | tojson }},
-    batches:             {{ batches_json | tojson }},
-    availableFields:     AVAILABLE_FIELDS,
-    recipesIngredients:  {{ recipes_ingredients | tojson }},
-    ingredientsSummary:  '',
-    selectedRecipeId:    '',
-
-    // ÔöÇÔöÇ Print state ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    printBatchId:     '',
-    printQty:         1,
-    printPreviewHtml: '',
-
-    // ÔöÇÔöÇ Batch form state ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    batchNumber:   '',
-    validadeMeses: 3,
-    expiryDate:    '',
-
-    // ÔöÇÔöÇ Computed dimensions ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    get canvasW() { return Math.round(this.widthMm  * this.zoom); },
+  // ÔöÇÔöÇ Sample data shown in canvas preview ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  const SAMPLE = {
+    product_name: 'Frango Grelhado',
+    batch_number: 'L-2024-001',
+    production_date: '01/04/2024',
+    expiry_date: '08/04/2024',
+    weight: '1.250 kg',
+    ingredients_summary: 'Frango, Alho, Azeite, Sal',
+  };
+
+  const FIELD_ICONS = {
+    product_name: '­ƒôª', batch_number: '­ƒöó', production_date: '­ƒôà',
+    expiry_date: 'ÔÅ│', weight: 'ÔÜû´©Å', ingredients_summary: '­ƒÑù', qr_code: 'Ôûú',
+  };
+
+  // Available field types the user can add
+  const AVAILABLE_FIELDS = [
+    { type: 'product_name', label: 'Nome do Produto', isQr: false, font_size_mm: 4.5, bold: true },
+    { type: 'batch_number', label: 'N├║mero do Lote', isQr: false, font_size_mm: 3, bold: false },
+    { type: 'production_date', label: 'Data de Fabrica├º├úo', isQr: false, font_size_mm: 3, bold: false },
+    { type: 'expiry_date', label: 'Data de Validade', isQr: false, font_size_mm: 3, bold: true },
+    { type: 'weight', label: 'Peso', isQr: false, font_size_mm: 3, bold: false },
+    { type: 'ingredients_summary', label: 'Lista de Ingredientes', isQr: false, font_size_mm: 2.5, bold: false },
+    { type: 'qr_code', label: 'QR Code Din├ómico', isQr: true, size: 15 },
+  ];
+
+  function labelMaker() {
+    return {
+      // ÔöÇÔöÇ Template state ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+      currentId: null,
+      tplName: '',
+      widthMm: 62,
+      heightMm: 40,
+      printerType: 'ZPL',
+      printerIp: '',
+      printerPort: 9100,
+      fields: [],
+
+      // ÔöÇÔöÇ UI state ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+      selectedIdx: null,
+      saving: false,
+      zoom: 7,        // px per mm
+      previewBatchIdx: -1,
+      showTemplates: true,
+      savedTemplates: {{ templates_json | tojson }
+  },
+  batches: { { batches_json | tojson } },
+  availableFields: AVAILABLE_FIELDS,
+    recipesIngredients: { { recipes_ingredients | tojson } },
+  ingredientsSummary: '',
+    selectedRecipeId: '',
+
+      // ÔöÇÔöÇ Print state ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+      printBatchId: '',
+        printQty: 1,
+          printPreviewHtml: '',
+
+            // ÔöÇÔöÇ Batch form state ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+            batchNumber: '',
+              validadeMeses: 3,
+                expiryDate: '',
+
+                  // ÔöÇÔöÇ Computed dimensions ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+                  get canvasW() { return Math.round(this.widthMm * this.zoom); },
     get canvasH() { return Math.round(this.heightMm * this.zoom); },
 
-    // ÔöÇÔöÇ Init ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    init() {
-      this.generateBatchNumber();
-      this.calcExpiry();
-      // Load most-recently saved template on page load (if any)
-      if (this.savedTemplates.length) {
-        this.loadTemplate(this.savedTemplates[0]);
-      }
-    },
+  // ÔöÇÔöÇ Init ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  init() {
+    this.generateBatchNumber();
+    this.calcExpiry();
+    // Load most-recently saved template on page load (if any)
+    if (this.savedTemplates.length) {
+      this.loadTemplate(this.savedTemplates[0]);
+    }
+  },
 
     // ÔöÇÔöÇ Print preview ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
     async fetchPrintPreview() {
-      if (!this.currentId || !this.printBatchId) { this.printPreviewHtml = ''; return; }
-      try {
-        const res = await fetch(`/labels/${this.currentId}/preview?batch_id=${this.printBatchId}`);
-        this.printPreviewHtml = res.ok ? await res.text() : '';
-      } catch (_) { this.printPreviewHtml = ''; }
-    },
-
-    // ÔöÇÔöÇ Batch helpers ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    addBatchFromResponse(event, form) {
-      const fd = new FormData(form);
-      // Parse batch ID from the returned HTML row: id="batch-123"
-      const doc = new DOMParser().parseFromString(event.detail.xhr.responseText, 'text/html');
-      const row = doc.querySelector('[id^="batch-"]');
-      const batchId = row ? parseInt(row.id.split('-')[1]) : Date.now();
-      this.batches.unshift({
-        id:                  batchId,
-        batch_number:        this.batchNumber,
-        product_name:        fd.get('product_name') || '',
-        production_date:     new Date().toLocaleDateString('pt-BR'),
-        expiry_date:         this.expiryDate.split('-').reverse().join('/'),
-        weight_kg:           parseFloat(fd.get('weight_kg')) || 0,
-        ingredients_summary: fd.get('ingredients_summary') || '',
-      });
-      form.reset();
-      this.generateBatchNumber();
-      this.calcExpiry();
-      toast('Lote registrado');
-    },
-
-    generateBatchNumber() {
-      const d = new Date();
-      const yyyy = d.getFullYear();
-      const mm   = String(d.getMonth() + 1).padStart(2, '0');
-      const dd   = String(d.getDate()).padStart(2, '0');
-      const rnd  = Math.floor(Math.random() * 1000).toString().padStart(3, '0');
-      this.batchNumber = `L-${yyyy}${mm}${dd}-${rnd}`;
-    },
-
-    calcExpiry() {
-      const d = new Date();
-      d.setMonth(d.getMonth() + parseInt(this.validadeMeses) || 3);
-      this.expiryDate = d.toISOString().split('T')[0];
-    },
-
-    // ÔöÇÔöÇ Field helpers ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    fieldIcon(type) { return FIELD_ICONS[type] || '?'; },
-
-    sampleText(type) {
-      const batch = this.previewBatchIdx >= 0 ? this.batches[this.previewBatchIdx] : null;
-      const raw = batch ? {
-        product_name:        batch.product_name,
-        batch_number:        batch.batch_number,
-        production_date:     batch.production_date,
-        expiry_date:         batch.expiry_date,
-        weight:              batch.weight_kg + ' kg',
-        ingredients_summary: batch.ingredients_summary,
-      } : SAMPLE;
-      const prefixes = {
-        batch_number:        'Lote: ',
-        expiry_date:         'Validade: ',
-        production_date:     'Fabrica├º├úo: ',
-        ingredients_summary: 'Ingredientes: ',
-      };
-      const val = raw[type] || type;
-      return (prefixes[type] || '') + val;
-    },
-
-    fieldStyle(f) {
-      const s = this.zoom;
+    if (!this.currentId || !this.printBatchId) { this.printPreviewHtml = ''; return; }
+    try {
+      const res = await fetch(`/labels/${this.currentId}/preview?batch_id=${this.printBatchId}`);
+      this.printPreviewHtml = res.ok ? await res.text() : '';
+    } catch (_) { this.printPreviewHtml = ''; }
+  },
+
+  // ÔöÇÔöÇ Batch helpers ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  addBatchFromResponse(event, form) {
+    const fd = new FormData(form);
+    // Parse batch ID from the returned HTML row: id="batch-123"
+    const doc = new DOMParser().parseFromString(event.detail.xhr.responseText, 'text/html');
+    const row = doc.querySelector('[id^="batch-"]');
+    const batchId = row ? parseInt(row.id.split('-')[1]) : Date.now();
+    this.batches.unshift({
+      id: batchId,
+      batch_number: this.batchNumber,
+      product_name: fd.get('product_name') || '',
+      production_date: new Date().toLocaleDateString('pt-BR'),
+      expiry_date: this.expiryDate.split('-').reverse().join('/'),
+      weight_kg: parseFloat(fd.get('weight_kg')) || 0,
+      ingredients_summary: fd.get('ingredients_summary') || '',
+    });
+    form.reset();
+    this.generateBatchNumber();
+    this.calcExpiry();
+    toast('Lote registrado');
+  },
+
+  generateBatchNumber() {
+    const d = new Date();
+    const yyyy = d.getFullYear();
+    const mm = String(d.getMonth() + 1).padStart(2, '0');
+    const dd = String(d.getDate()).padStart(2, '0');
+    const rnd = Math.floor(Math.random() * 1000).toString().padStart(3, '0');
+    this.batchNumber = `L-${yyyy}${mm}${dd}-${rnd}`;
+  },
+
+  calcExpiry() {
+    const d = new Date();
+    d.setMonth(d.getMonth() + parseInt(this.validadeMeses) || 3);
+    this.expiryDate = d.toISOString().split('T')[0];
+  },
+
+  // ÔöÇÔöÇ Field helpers ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  fieldIcon(type) { return FIELD_ICONS[type] || '?'; },
+
+  sampleText(type) {
+    const batch = this.previewBatchIdx >= 0 ? this.batches[this.previewBatchIdx] : null;
+    const raw = batch ? {
+      product_name: batch.product_name,
+      batch_number: batch.batch_number,
+      production_date: batch.production_date,
+      expiry_date: batch.expiry_date,
+      weight: batch.weight_kg + ' kg',
+      ingredients_summary: batch.ingredients_summary,
+    } : SAMPLE;
+    const prefixes = {
+      batch_number: 'Lote: ',
+      expiry_date: 'Validade: ',
+      production_date: 'Fabrica├º├úo: ',
+      ingredients_summary: 'Ingredientes: ',
+    };
+    const val = raw[type] || type;
+    return (prefixes[type] || '') + val;
+  },
+
+  fieldStyle(f) {
+    const s = this.zoom;
+    if (f.isQr) {
+      const sz = Math.round((f.size || 15) * s);
+      return `left:${Math.round(f.x * s)}px; top:${Math.round(f.y * s)}px; width:${sz}px; height:${sz}px;`;
+    }
+    const fontSize = (f.font_size_mm || 3) * s * 0.8;   // 0.8 Ôëê CSS px / mm at screen resolution
+    return `left:${Math.round(f.x * s)}px; top:${Math.round(f.y * s)}px; `
+      + `font-size:${fontSize.toFixed(1)}px; `
+      + `font-weight:${f.bold ? 'bold' : 'normal'};`;
+  },
+
+  // ÔöÇÔöÇ Add / remove fields ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  addField(af) {
+    // Place new field offset from origin so it's visible
+    const offset = this.fields.length * 3;
+    const base = { x: 2 + offset, y: 2 + offset, bold: af.bold || false, label: af.label };
+    if (af.isQr) {
+      this.fields.push({ ...base, type: af.type, isQr: true, size: af.size || 15 });
+    } else {
+      this.fields.push({ ...base, type: af.type, isQr: false, font_size_mm: af.font_size_mm || 3 });
+    }
+    this.selectedIdx = this.fields.length - 1;
+  },
+
+  removeField(i) {
+    this.fields.splice(i, 1);
+    if (this.selectedIdx >= this.fields.length) this.selectedIdx = null;
+  },
+
+  // Clamp field positions when canvas dimensions change
+  clampFields() {
+    for (const f of this.fields) {
+      f.x = Math.max(0, Math.min(f.x, this.widthMm - 1));
+      f.y = Math.max(0, Math.min(f.y, this.heightMm - 1));
+    }
+  },
+
+  // ÔöÇÔöÇ Drag and drop ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  startDrag(e, index) {
+    this.selectedIdx = index;
+    const field = this.fields[index];
+    const canvas = this.$refs.canvas;
+    const rect = canvas.getBoundingClientRect();
+    const s = this.zoom;
+
+    // Calcula onde o mouse clicou dentro do pr├│prio elemento
+    const elRect = e.currentTarget.getBoundingClientRect();
+    const offsetX = e.clientX - elRect.left;
+    const offsetY = e.clientY - elRect.top;
+
+    const onMove = (ev) => {
+      const newX = (ev.clientX - rect.left - offsetX) / s;
+      const newY = (ev.clientY - rect.top - offsetY) / s;
+      field.x = Math.round(newX * 10) / 10;
+      field.y = Math.round(newY * 10) / 10;
+      ev.preventDefault();
+    };
+    const onUp = () => {
+      document.removeEventListener('mousemove', onMove);
+      document.removeEventListener('mouseup', onUp);
+    };
+    document.addEventListener('mousemove', onMove);
+    document.addEventListener('mouseup', onUp);
+    e.preventDefault();
+  },
+
+  // ÔöÇÔöÇ Keyboard nudge (arrow keys) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  nudge(dx, dy) {
+    if (this.selectedIdx === null) return;
+    const f = this.fields[this.selectedIdx];
+    if (!f) return;
+    f.x = Math.max(0, Math.min(Math.round((f.x + dx) * 10) / 10, this.widthMm - 0.5));
+    f.y = Math.max(0, Math.min(Math.round((f.y + dy) * 10) / 10, this.heightMm - 0.5));
+  },
+
+  // ÔöÇÔöÇ Build fields_config JSON for backend ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  buildConfig() {
+    return this.fields.map(f => {
+      const cfg = { field: f.type, x: f.x, y: f.y };
       if (f.isQr) {
-        const sz = Math.round((f.size || 15) * s);
-        return `left:${Math.round(f.x * s)}px; top:${Math.round(f.y * s)}px; width:${sz}px; height:${sz}px;`;
-      }
-      const fontSize = (f.font_size_mm || 3) * s * 0.8;   // 0.8 Ôëê CSS px / mm at screen resolution
-      return `left:${Math.round(f.x * s)}px; top:${Math.round(f.y * s)}px; `
-           + `font-size:${fontSize.toFixed(1)}px; `
-           + `font-weight:${f.bold ? 'bold' : 'normal'};`;
-    },
-
-    // ÔöÇÔöÇ Add / remove fields ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    addField(af) {
-      // Place new field offset from origin so it's visible
-      const offset = this.fields.length * 3;
-      const base = { x: 2 + offset, y: 2 + offset, bold: af.bold || false, label: af.label };
-      if (af.isQr) {
-        this.fields.push({ ...base, type: af.type, isQr: true, size: af.size || 15 });
+        cfg.size = f.size || 15;
       } else {
-        this.fields.push({ ...base, type: af.type, isQr: false, font_size_mm: af.font_size_mm || 3 });
+        cfg.font_size_mm = f.font_size_mm || 3;
+        cfg.bold = f.bold;
       }
-      this.selectedIdx = this.fields.length - 1;
-    },
-
-    removeField(i) {
-      this.fields.splice(i, 1);
-      if (this.selectedIdx >= this.fields.length) this.selectedIdx = null;
-    },
-
-    // Clamp field positions when canvas dimensions change
-    clampFields() {
-      for (const f of this.fields) {
-        f.x = Math.max(0, Math.min(f.x, this.widthMm  - 1));
-        f.y = Math.max(0, Math.min(f.y, this.heightMm - 1));
-      }
-    },
-
-    // ÔöÇÔöÇ Drag and drop ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    startDrag(e, index) {
-      this.selectedIdx = index;
-      const field = this.fields[index];
-      const canvas = this.$refs.canvas;
-      const rect   = canvas.getBoundingClientRect();
-      const s      = this.zoom;
-
-      // Calcula onde o mouse clicou dentro do pr├│prio elemento
-      const elRect = e.currentTarget.getBoundingClientRect();
-      const offsetX = e.clientX - elRect.left;
-      const offsetY = e.clientY - elRect.top;
-
-      const onMove = (ev) => {
-        const newX = (ev.clientX - rect.left - offsetX) / s;
-        const newY = (ev.clientY - rect.top  - offsetY) / s;
-        field.x = Math.round(newX * 10) / 10;
-        field.y = Math.round(newY * 10) / 10;
-        ev.preventDefault();
-      };
-      const onUp = () => {
-        document.removeEventListener('mousemove', onMove);
-        document.removeEventListener('mouseup',   onUp);
-      };
-      document.addEventListener('mousemove', onMove);
-      document.addEventListener('mouseup',   onUp);
-      e.preventDefault();
-    },
-
-    // ÔöÇÔöÇ Keyboard nudge (arrow keys) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    nudge(dx, dy) {
-      if (this.selectedIdx === null) return;
-      const f = this.fields[this.selectedIdx];
-      if (!f) return;
-      f.x = Math.max(0, Math.min(Math.round((f.x + dx) * 10) / 10, this.widthMm  - 0.5));
-      f.y = Math.max(0, Math.min(Math.round((f.y + dy) * 10) / 10, this.heightMm - 0.5));
-    },
-
-    // ÔöÇÔöÇ Build fields_config JSON for backend ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    buildConfig() {
-      return this.fields.map(f => {
-        const cfg = { field: f.type, x: f.x, y: f.y };
-        if (f.isQr) {
-          cfg.size = f.size || 15;
-        } else {
-          cfg.font_size_mm = f.font_size_mm || 3;
-          cfg.bold = f.bold;
-        }
-        return cfg;
-      });
-    },
+      return cfg;
+    });
+  },
 
     // ÔöÇÔöÇ Save / Update template ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
     async saveTemplate() {
-      if (!this.tplName.trim()) {
-        toast('Informe o nome do template', 'error'); return;
-      }
-      this.saving = true;
-      const body = new FormData();
-      body.append('name',          this.tplName);
-      body.append('width_mm',      this.widthMm);
-      body.append('height_mm',     this.heightMm);
-      body.append('printer_type',  this.printerType);
-      body.append('printer_ip',    this.printerIp);
-      body.append('printer_port',  this.printerPort);
-      body.append('fields_config', JSON.stringify(this.buildConfig()));
-
-      const method = this.currentId ? 'PUT' : 'POST';
-      const url    = this.currentId ? `/labels/${this.currentId}` : '/labels';
-
-      try {
-        const resp = await fetch(url, { method, body });
-        if (!resp.ok) throw new Error('Erro ao salvar');
-        const data = await resp.json();
-        this.currentId = data.id;
-        // Update or add to savedTemplates list
-        const idx = this.savedTemplates.findIndex(t => t.id === data.id);
-        if (idx >= 0) this.savedTemplates[idx] = data;
-        else          this.savedTemplates.unshift(data);
-        toast(this.currentId ? 'Template atualizado!' : 'Template salvo!');
-      } catch (err) {
-        toast(err.message, 'error');
-      } finally {
-        this.saving = false;
-      }
-    },
-
-    // ÔöÇÔöÇ Load a saved template into the editor ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    loadTemplate(t) {
-      this.currentId   = t.id;
-      this.tplName     = t.name;
-      this.widthMm     = t.width_mm;
-      this.heightMm    = t.height_mm;
-      this.printerType = t.printer_type;
-      this.printerIp   = t.printer_ip || '';
-      this.printerPort = t.printer_port;
-      // Parse fields_config JSON
-      try {
-        const cfg = JSON.parse(t.fields_config || '[]');
-        this.fields = cfg.map(c => {
-          const af = AVAILABLE_FIELDS.find(a => a.type === c.field) || {};
-          return c.field === 'qr_code'
-            ? { type: c.field, label: af.label || c.field, isQr: true,
-                x: c.x || 0, y: c.y || 0, size: c.size || 15 }
-            : { type: c.field, label: af.label || c.field, isQr: false,
-                x: c.x || 0, y: c.y || 0,
-                font_size_mm: c.font_size_mm || 3, bold: c.bold || false };
-        });
-      } catch (_) { this.fields = []; }
-      this.selectedIdx = null;
-    },
-
-    loadTemplateById(id) {
-      const t = this.savedTemplates.find(t => t.id === id);
-      if (t) this.loadTemplate(t);
-    },
-
-    // ÔöÇÔöÇ New blank template ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
-    newTemplate() {
-      this.currentId = null; this.tplName = ''; this.fields = [];
-      this.widthMm = 62; this.heightMm = 40;
-      this.printerType = 'ZPL'; this.printerIp = ''; this.printerPort = 9100;
-      this.selectedIdx = null;
-    },
+    if (!this.tplName.trim()) {
+      toast('Informe o nome do template', 'error'); return;
+    }
+    this.saving = true;
+    const body = new FormData();
+    body.append('name', this.tplName);
+    body.append('width_mm', this.widthMm);
+    body.append('height_mm', this.heightMm);
+    body.append('printer_type', this.printerType);
+    body.append('printer_ip', this.printerIp);
+    body.append('printer_port', this.printerPort);
+    body.append('fields_config', JSON.stringify(this.buildConfig()));
+
+    const method = this.currentId ? 'PUT' : 'POST';
+    const url = this.currentId ? `/labels/${this.currentId}` : '/labels';
+
+    try {
+      const resp = await fetch(url, { method, body });
+      if (!resp.ok) throw new Error('Erro ao salvar');
+      const data = await resp.json();
+      this.currentId = data.id;
+      // Update or add to savedTemplates list
+      const idx = this.savedTemplates.findIndex(t => t.id === data.id);
+      if (idx >= 0) this.savedTemplates[idx] = data;
+      else this.savedTemplates.unshift(data);
+      toast(this.currentId ? 'Template atualizado!' : 'Template salvo!');
+    } catch (err) {
+      toast(err.message, 'error');
+    } finally {
+      this.saving = false;
+    }
+  },
+
+  // ÔöÇÔöÇ Load a saved template into the editor ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  loadTemplate(t) {
+    this.currentId = t.id;
+    this.tplName = t.name;
+    this.widthMm = t.width_mm;
+    this.heightMm = t.height_mm;
+    this.printerType = t.printer_type;
+    this.printerIp = t.printer_ip || '';
+    this.printerPort = t.printer_port;
+    // Parse fields_config JSON
+    try {
+      const cfg = JSON.parse(t.fields_config || '[]');
+      this.fields = cfg.map(c => {
+        const af = AVAILABLE_FIELDS.find(a => a.type === c.field) || {};
+        return c.field === 'qr_code'
+          ? {
+            type: c.field, label: af.label || c.field, isQr: true,
+            x: c.x || 0, y: c.y || 0, size: c.size || 15
+          }
+          : {
+            type: c.field, label: af.label || c.field, isQr: false,
+            x: c.x || 0, y: c.y || 0,
+            font_size_mm: c.font_size_mm || 3, bold: c.bold || false
+          };
+      });
+    } catch (_) { this.fields = []; }
+    this.selectedIdx = null;
+  },
+
+  loadTemplateById(id) {
+    const t = this.savedTemplates.find(t => t.id === id);
+    if (t) this.loadTemplate(t);
+  },
+
+  // ÔöÇÔöÇ New blank template ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+  newTemplate() {
+    this.currentId = null; this.tplName = ''; this.fields = [];
+    this.widthMm = 62; this.heightMm = 40;
+    this.printerType = 'ZPL'; this.printerIp = ''; this.printerPort = 9100;
+    this.selectedIdx = null;
+  },
 
     // ÔöÇÔöÇ Delete template ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
     async deleteTemplate() {
-      if (!this.currentId) return;
-      if (!confirm(`Excluir template "${this.tplName}"?`)) return;
-      try {
-        const resp = await fetch(`/labels/${this.currentId}`, { method: 'DELETE' });
-        if (!resp.ok) throw new Error('Erro ao excluir');
-        this.savedTemplates = this.savedTemplates.filter(t => t.id !== this.currentId);
-        this.newTemplate();
-        toast('Template exclu├¡do', 'info');
-      } catch (err) {
-        toast(err.message, 'error');
-      }
-    },
+    if (!this.currentId) return;
+    if (!confirm(`Excluir template "${this.tplName}"?`)) return;
+    try {
+      const resp = await fetch(`/labels/${this.currentId}`, { method: 'DELETE' });
+      if (!resp.ok) throw new Error('Erro ao excluir');
+      this.savedTemplates = this.savedTemplates.filter(t => t.id !== this.currentId);
+      this.newTemplate();
+      toast('Template exclu├¡do', 'info');
+    } catch (err) {
+      toast(err.message, 'error');
+    }
+  },
 
     // ÔöÇÔöÇ Show ZPL/TSPL command ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
     async showCommand() {
-      if (!this.currentId) return;
-      htmx.ajax('GET', `/labels/${this.currentId}/command`, {
-        target: '#command-output', swap: 'innerHTML',
-      });
-    },
+    if (!this.currentId) return;
+    htmx.ajax('GET', `/labels/${this.currentId}/command`, {
+      target: '#command-output', swap: 'innerHTML',
+    });
+  },
   };
 }
 
-// Arrow key nudge for selected field
-document.addEventListener('keydown', (e) => {
-  if (!['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].includes(e.key)) return;
-  if (document.activeElement && document.activeElement.tagName === 'INPUT') return;
-  const comp = document.querySelector('[x-data]').__x?.$data;
-  if (!comp || comp.selectedIdx === null) return;
-  const step = e.shiftKey ? 1 : 0.5;
-  if (e.key === 'ArrowLeft')  comp.nudge(-step, 0);
-  if (e.key === 'ArrowRight') comp.nudge( step, 0);
-  if (e.key === 'ArrowUp')    comp.nudge(0, -step);
-  if (e.key === 'ArrowDown')  comp.nudge(0,  step);
-  e.preventDefault();
-});
+  // Arrow key nudge for selected field
+  document.addEventListener('keydown', (e) => {
+    if (!['ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown'].includes(e.key)) return;
+    if (document.activeElement && document.activeElement.tagName === 'INPUT') return;
+    const comp = document.querySelector('[x-data]').__x?.$data;
+    if (!comp || comp.selectedIdx === null) return;
+    const step = e.shiftKey ? 1 : 0.5;
+    if (e.key === 'ArrowLeft') comp.nudge(-step, 0);
+    if (e.key === 'ArrowRight') comp.nudge(step, 0);
+    if (e.key === 'ArrowUp') comp.nudge(0, -step);
+    if (e.key === 'ArrowDown') comp.nudge(0, step);
+    e.preventDefault();
+  });
 </script>
-{% endblock %}
+{% endblock %}
\ No newline at end of file
